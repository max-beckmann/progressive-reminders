import { Routes } from '@angular/router';
import { NewPageComponent } from './components/pages/new/new-page.component';

export const routes: Routes = [
  { path: 'new', component: NewPageComponent }
];

